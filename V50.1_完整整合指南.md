# V50.1 完整整合指南

## 🚀 **整合步驟**

### **步驟 1: 更新 CONFIG 配置**
在你的 `Code.gs` 文件的 CONFIG 部分，新增 Telegram Bot Token：

```javascript
const CONFIG = {
  // ... 現有配置保持不變
  
  // V50.1 新增: Bot Token
  TELEGRAM_BOT_TOKEN: PropertiesService.getScriptProperties().getProperty('TELEGRAM_BOT_TOKEN') || 'YOUR_TELEGRAM_BOT_TOKEN_HERE',
  
  // ... 其他配置
};
```

### **步驟 2: 更新 doPost 函數**
修改現有的 `doPost` 函數，新增 Bot 請求檢測：

```javascript
function doPost(e) {
  return safeExecute(() => {
    // V50.1 新增: Bot Webhook 優先處理
    try {
      const contents = JSON.parse(e.postData.contents);
      if (contents.update_id) {
        Logger.log("[V50.1-POST] 偵測到 Telegram Webhook，轉發至 Bot 處理器。");
        return doPost_Bot(e);
      }
    } catch(err) {
      // 解析失敗，不是 Bot 的請求，繼續正常流程
    }

    // 原有的邏輯保持不變
    if (!e.parameter) { throw new Error('缺少請求參數'); }
    
    const endpoint = e.parameter.endpoint;
    if (!endpoint) { throw new Error('缺少 endpoint 參數'); }
    
    if (endpoint === 'image') { return doPost_Image(e); }
    else if (endpoint === 'voice') { return doPost_Voice(e); }
    else if (endpoint === 'pdf') { return doPost_Pdf(e); }
    else if (endpoint === 'iou') { return doPost_Iou(e); }
    // V50.1 新增: Bot 路由
    else if (endpoint === 'bot') { return doPost_Bot(e); }
    else { throw new Error(`無效的 API 端點: ${endpoint}`); }
  }, { name: 'doPost' });
}
```

### **步驟 3: 新增 Bot 處理函數**
將以下函數新增到你的 `Code.gs` 文件末尾：

1. **用戶狀態常數** (在文件開頭新增)
2. **Bot 核心處理邏輯** (從 `V50.1_Bot核心處理邏輯.gs` 複製)
3. **圖片語音處理邏輯** (從 `V50.1_圖片語音處理邏輯.gs` 複製)
4. **狀態管理與工具函數** (從 `V50.1_狀態管理與工具函數.gs` 複製)

### **步驟 4: 設定 Webhook**
1. 部署你的 Google Apps Script 為 Web App
2. 複製 Web App URL
3. 設定 Telegram Bot Webhook：

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url": "https://script.google.com/macros/s/<YOUR_SCRIPT_ID>/exec"}'
```

## 🧪 **測試步驟**

### **基本測試**
1. **發送 `/start`** - 應該收到歡迎訊息
2. **發送 `/help`** - 應該收到使用說明
3. **發送文字** - 如「午餐 150 元」，應該收到確認訊息
4. **發送圖片** - 應該自動識別並請求確認
5. **發送語音** - 應該處理語音並請求確認

### **確認流程測試**
1. 發送記帳資訊後，點擊「✅ 確認記帳」按鈕
2. 檢查 Google Sheets 是否有新記錄
3. 測試「❌ 取消」按鈕功能

### **錯誤處理測試**
1. 發送無法識別的圖片
2. 發送不清楚的語音
3. 發送無意義的文字

## ⚙️ **配置檢查清單**

### **Google Apps Script 設定**
- [ ] `TELEGRAM_BOT_TOKEN` 已設定在指令碼屬性中
- [ ] `MAIN_LEDGER_ID` 已正確設定
- [ ] `GEMINI_API_KEY` 已正確設定
- [ ] Web App 已部署並設定為「任何人」可存取

### **Telegram Bot 設定**
- [ ] Bot 已透過 @BotFather 創建
- [ ] Bot Token 已複製並設定
- [ ] Webhook 已正確設定
- [ ] Bot 可以接收訊息

### **權限設定**
- [ ] Google Sheets 存取權限正常
- [ ] Gmail 存取權限正常 (如果使用郵件功能)
- [ ] Google Drive 存取權限正常 (如果使用圖片存檔)

## 🔧 **故障排除**

### **常見問題**

#### **1. Bot 沒有回應**
- 檢查 Webhook 是否正確設定
- 檢查 Google Apps Script 執行日誌
- 確認 `TELEGRAM_BOT_TOKEN` 設定正確

#### **2. 圖片處理失敗**
- 檢查 `GEMINI_API_KEY` 是否正確
- 確認圖片大小不超過限制
- 檢查網路連線狀況

#### **3. 記帳失敗**
- 檢查 `MAIN_LEDGER_ID` 是否正確
- 確認 Google Sheets 存取權限
- 檢查試算表結構是否正確

### **調試方法**
1. **查看執行日誌**: Google Apps Script 編輯器 → 執行 → 查看執行記錄
2. **測試單一函數**: 在編輯器中直接執行特定函數
3. **檢查 Webhook**: 使用 `getWebhookInfo` API 檢查設定

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

## 📊 **功能對照表**

| 功能 | 狀態 | 說明 |
|------|------|------|
| 基本指令 | ✅ | `/start`, `/help`, `/stats`, `/cancel` |
| 文字記帳 | ✅ | 直接輸入消費資訊 |
| 圖片記帳 | ✅ | 拍照自動識別 |
| 語音記帳 | ✅ | 語音轉文字記帳 |
| 確認流程 | ✅ | 互動式確認按鈕 |
| 狀態管理 | ✅ | 多輪對話支援 |
| 錯誤處理 | ✅ | 友善的錯誤提示 |
| 統計查詢 | 🔄 | 基礎版本，可擴展 |
| 編輯功能 | 🔄 | 預留接口，待完善 |

## 🎯 **下一步發展**

### **短期改進 (1-2 週)**
- [ ] 完善統計查詢功能
- [ ] 新增編輯記錄功能
- [ ] 優化錯誤處理
- [ ] 新增更多快捷指令

### **中期目標 (1 個月)**
- [ ] 新增 Line Bot 支援
- [ ] 實作群組記帳功能
- [ ] 新增預算管理
- [ ] 優化 AI 識別準確率

### **長期願景 (3 個月)**
- [ ] 智慧分析和建議
- [ ] 多平台資料同步
- [ ] 進階報表功能
- [ ] 商家整合

---

## 🎉 **完成確認**

當你完成所有整合步驟後，你的智慧記帳系統將具備：

- 🤖 **Telegram Bot 整合**: 隨時隨地記帳
- 📸 **智慧圖片識別**: AI 自動分析收據
- 🎤 **語音記帳**: 說話就能記帳
- ✏️ **文字記帳**: 簡單輸入即可
- 🔄 **互動確認**: 確保記帳準確性
- 📊 **統計查詢**: 隨時查看消費狀況

**恭喜！你現在擁有一個功能完整的 Bot 整合記帳系統！** 🎊

---

*整合指南版本: v1.0*  
*更新日期: 2025-10-12*  
*適用版本: V50.1.0*