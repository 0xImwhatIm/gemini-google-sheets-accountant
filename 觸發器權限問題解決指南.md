# 觸發器權限問題解決指南

## 🚨 **問題分析**

### **錯誤訊息**
```
Authorization is required to perform that action.
```

### **問題原因**
1. **權限過期**: Google Apps Script 的授權 token 過期
2. **觸發器權限**: 時間觸發器需要重新授權才能存取外部服務
3. **服務範圍**: Gmail 和 Google Sheets 存取權限需要重新確認

### **影響範圍**
- ✅ **手動執行**: 正常運作
- ❌ **自動觸發**: 權限錯誤
- ❌ **郵件處理**: 無法存取 Gmail
- ❌ **資料寫入**: 無法存取 Google Sheets

## 🔧 **解決方案**

### **方案一：重新授權 (推薦)**

#### **步驟 1: 手動執行函數重新授權**
1. 開啟 [Google Apps Script 編輯器](https://script.google.com/)
2. 選擇你的專案
3. 在編輯器中選擇 `processAutomatedEmails` 函數
4. 點擊「執行」按鈕 (▶️)
5. 系統會彈出授權對話框，點擊「檢閱權限」
6. 選擇你的 Google 帳戶
7. 點擊「允許」授權所有權限

#### **步驟 2: 確認權限範圍**
確保授權了以下權限：
- ✅ **Gmail**: 讀取、搜尋、標記郵件
- ✅ **Google Sheets**: 讀取、寫入試算表
- ✅ **Google Drive**: 存取檔案 (用於圖片上傳)
- ✅ **外部連接**: 呼叫 Gemini API

#### **步驟 3: 測試觸發器**
```javascript
// 在編輯器中執行此函數測試權限
function testTriggerPermissions() {
  try {
    // 測試 Gmail 存取
    const threads = GmailApp.search('is:unread', 0, 1);
    Logger.log('✅ Gmail 存取正常');
    
    // 測試 Sheets 存取
    const ss = SpreadsheetApp.openById(CONFIG.MAIN_LEDGER_ID);
    Logger.log('✅ Sheets 存取正常');
    
    // 測試郵件處理
    processAutomatedEmails();
    Logger.log('✅ 郵件處理正常');
    
    return true;
  } catch (error) {
    Logger.log(`❌ 權限測試失敗: ${error.message}`);
    return false;
  }
}
```

### **方案二：重建觸發器**

#### **步驟 1: 刪除現有觸發器**
```javascript
// 執行此函數刪除所有觸發器
function deleteAllTriggers() {
  const triggers = ScriptApp.getProjectTriggers();
  triggers.forEach(trigger => {
    ScriptApp.deleteTrigger(trigger);
    Logger.log(`已刪除觸發器: ${trigger.getHandlerFunction()}`);
  });
  Logger.log(`✅ 已刪除 ${triggers.length} 個觸發器`);
}
```

#### **步驟 2: 重新創建觸發器**
```javascript
// 執行此函數重新創建觸發器
function recreateTriggers() {
  // 刪除現有觸發器
  deleteAllTriggers();
  
  // 創建郵件處理觸發器 (每5分鐘)
  ScriptApp.newTrigger('processAutomatedEmails')
    .timeBased()
    .everyMinutes(5)
    .create();
  
  // 創建系統健康檢查觸發器 (每日)
  ScriptApp.newTrigger('checkSystemHealth')
    .timeBased()
    .everyDays(1)
    .atHour(9)
    .create();
  
  Logger.log('✅ 觸發器重新創建完成');
  
  // 立即測試
  testTriggerPermissions();
}
```

### **方案三：使用安全的郵件處理函數**

#### **修改觸發器使用安全函數**
```javascript
// 創建使用安全函數的觸發器
function createSafeTriggers() {
  // 刪除現有觸發器
  deleteAllTriggers();
  
  // 使用安全的郵件處理函數
  ScriptApp.newTrigger('safeProcessAutomatedEmails')
    .timeBased()
    .everyMinutes(5)
    .create();
  
  Logger.log('✅ 安全觸發器創建完成');
}
```

## 🛠️ **詳細操作步驟**

### **立即修復步驟**

#### **1. 重新授權 (必須)**
```javascript
// 在 Google Apps Script 編輯器中執行
function forceReauthorization() {
  Logger.log('🔐 開始重新授權程序...');
  
  try {
    // 強制觸發權限請求
    const testThread = GmailApp.search('is:unread', 0, 1);
    const testSheet = SpreadsheetApp.openById(CONFIG.MAIN_LEDGER_ID);
    
    Logger.log('✅ 重新授權成功');
    return true;
  } catch (error) {
    Logger.log(`❌ 重新授權失敗: ${error.message}`);
    Logger.log('💡 請手動點擊「執行」按鈕並完成授權流程');
    return false;
  }
}
```

#### **2. 重建觸發器 (推薦)**
```javascript
// 完整的觸發器重建函數
function completeTriggersRebuild() {
  Logger.log('🔄 開始完整觸發器重建...');
  
  // 步驟 1: 刪除所有現有觸發器
  const existingTriggers = ScriptApp.getProjectTriggers();
  existingTriggers.forEach(trigger => {
    ScriptApp.deleteTrigger(trigger);
  });
  Logger.log(`🗑️ 已刪除 ${existingTriggers.length} 個舊觸發器`);
  
  // 步驟 2: 等待一下
  Utilities.sleep(2000);
  
  // 步驟 3: 創建新觸發器
  try {
    // 郵件處理觸發器 - 每5分鐘
    const emailTrigger = ScriptApp.newTrigger('safeProcessAutomatedEmails')
      .timeBased()
      .everyMinutes(5)
      .create();
    Logger.log('✅ 郵件處理觸發器已創建');
    
    // 系統健康檢查觸發器 - 每日上午9點
    const healthTrigger = ScriptApp.newTrigger('checkSystemHealth')
      .timeBased()
      .everyDays(1)
      .atHour(9)
      .create();
    Logger.log('✅ 健康檢查觸發器已創建');
    
    // 步驟 4: 測試新觸發器
    Logger.log('🧪 測試新觸發器...');
    const testResult = testTriggerPermissions();
    
    if (testResult) {
      Logger.log('🎉 觸發器重建完成且測試通過！');
    } else {
      Logger.log('⚠️ 觸發器重建完成，但需要手動授權');
    }
    
    return true;
  } catch (error) {
    Logger.log(`❌ 觸發器創建失敗: ${error.message}`);
    return false;
  }
}
```

#### **3. 驗證修復結果**
```javascript
// 驗證修復是否成功
function verifyFix() {
  Logger.log('🔍 驗證修復結果...');
  
  // 檢查觸發器
  const triggers = ScriptApp.getProjectTriggers();
  Logger.log(`📋 當前觸發器數量: ${triggers.length}`);
  
  triggers.forEach((trigger, index) => {
    Logger.log(`  ${index + 1}. ${trigger.getHandlerFunction()} - ${trigger.getTriggerSource()}`);
  });
  
  // 測試權限
  const permissionTest = testTriggerPermissions();
  
  if (permissionTest) {
    Logger.log('✅ 修復成功！系統準備就緒');
  } else {
    Logger.log('❌ 仍有權限問題，請檢查授權狀態');
  }
  
  return permissionTest;
}
```

## 📋 **完整修復流程**

### **執行順序**
1. **forceReauthorization()** - 重新授權
2. **completeTriggersRebuild()** - 重建觸發器
3. **verifyFix()** - 驗證修復結果

### **預期結果**
- ✅ 觸發器正常執行
- ✅ 郵件自動處理恢復
- ✅ 錯誤日誌消失
- ✅ 系統穩定運行

## 🚨 **常見問題排除**

### **問題 1: 授權對話框沒有出現**
**解決方案:**
1. 清除瀏覽器快取
2. 使用無痕模式開啟 Google Apps Script
3. 確認 Google 帳戶登入狀態

### **問題 2: 授權後仍然出現錯誤**
**解決方案:**
```javascript
// 強制清除權限快取
function clearPermissionCache() {
  // 刪除所有觸發器
  const triggers = ScriptApp.getProjectTriggers();
  triggers.forEach(trigger => ScriptApp.deleteTrigger(trigger));
  
  // 等待 24 小時後重新創建觸發器
  Logger.log('⏰ 請等待 24 小時後重新創建觸發器');
}
```

### **問題 3: 特定服務權限被拒絕**
**解決方案:**
1. 檢查 `appsscript.json` 中的 OAuth 範圍
2. 確認所有必要的 API 都已啟用
3. 重新部署 Web App

## 🔒 **權限範圍確認**

### **必要的 OAuth 範圍**
```json
{
  "oauthScopes": [
    "https://www.googleapis.com/auth/gmail.readonly",
    "https://www.googleapis.com/auth/gmail.modify",
    "https://www.googleapis.com/auth/spreadsheets",
    "https://www.googleapis.com/auth/drive.file",
    "https://www.googleapis.com/auth/script.external_request"
  ]
}
```

### **檢查權限狀態**
```javascript
function checkPermissionStatus() {
  try {
    // 測試各項權限
    const gmailTest = GmailApp.search('is:unread', 0, 1);
    Logger.log('✅ Gmail 權限正常');
    
    const sheetsTest = SpreadsheetApp.openById(CONFIG.MAIN_LEDGER_ID);
    Logger.log('✅ Sheets 權限正常');
    
    const urlTest = UrlFetchApp.fetch('https://www.google.com');
    Logger.log('✅ 外部請求權限正常');
    
    return true;
  } catch (error) {
    Logger.log(`❌ 權限檢查失敗: ${error.message}`);
    return false;
  }
}
```

## 🎯 **預防措施**

### **定期維護**
```javascript
// 每月執行一次的維護函數
function monthlyMaintenance() {
  Logger.log('🔧 執行每月維護...');
  
  // 檢查觸發器狀態
  const triggers = ScriptApp.getProjectTriggers();
  Logger.log(`📋 當前觸發器: ${triggers.length} 個`);
  
  // 測試權限
  const permissionOK = checkPermissionStatus();
  
  // 清理執行日誌 (保留最近 30 天)
  console.clear();
  
  if (permissionOK) {
    Logger.log('✅ 系統狀態良好');
  } else {
    Logger.log('⚠️ 需要重新授權');
    forceReauthorization();
  }
}
```

### **監控設定**
```javascript
// 設定監控觸發器
function setupMonitoring() {
  // 每日檢查系統狀態
  ScriptApp.newTrigger('monthlyMaintenance')
    .timeBased()
    .everyDays(30)
    .atHour(8)
    .create();
  
  Logger.log('✅ 監控觸發器已設定');
}
```

---

## 🚀 **立即行動計劃**

### **現在就執行 (5分鐘)**
1. 開啟 Google Apps Script 編輯器
2. 複製並執行 `forceReauthorization()` 函數
3. 完成授權流程
4. 執行 `completeTriggersRebuild()` 函數
5. 執行 `verifyFix()` 確認修復

### **預期結果**
- ⏰ **立即**: 錯誤日誌停止產生
- ⏰ **5分鐘內**: 觸發器恢復正常執行
- ⏰ **15分鐘內**: 郵件自動處理恢復運作

**🎉 修復完成後，你的智慧記帳系統將恢復全自動運作！**